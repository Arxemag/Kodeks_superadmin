# Auth API, HTTP
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
h2>=4.0.0

# Config, DTOs, validation
pydantic>=2.0.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# DB
SQLAlchemy[asyncio]>=2.0.30
asyncpg>=0.29.0

# Kafka workers
aiokafka>=0.11.0

# Metrics
prometheus-client>=0.20.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.24.0

# init_company: загрузка формы ACL при JS-рендеринге каталога (опционально: pip install playwright && playwright install chromium)
playwright>=1.49.0
